# [HERMES_TO_TRAE_COMMAND]
**[TIMESTAMP]:** 2026-05-26T13:10:00+08:00
**[PROJECT]:** training-system
**[STATUS]:** EXECUTED_BY_HERMES (TRAE IDE+Solo双模式均静默忽略，Hermes用patch手动完成)

## 当前状态

项目路径: ~/Projects/training-system/
后端: FastAPI :8001, SQLite data/training.db
前端: Streamlit :8502 (主文件 streamlit_app.py, 985行)
题库: data/questions_seed.json (541题)
模块: 27个, 定义在 app/config.py MODULE_META

已有功能: 首页Dashboard、课程中心(仅显示卡片+阅读记录)、练习答题、进度追踪、错题本、成绩统计、知识搜索、可视化实验室(5个)、学生/教师双tab

## 需求

### P0 — 紧急 Bug修复

#### Bug 1: 习题练习"下一题"不跳转

**文件**: streamlit_app.py, quiz_active_page() 函数 (行625-670)

**根本原因**: 
"下一题"按钮 (行668) 嵌套在 `if st.button("提交答案") and answer:` 的代码块内部。当用户点击"下一题"时Streamlit重新运行脚本，此时 `st.button("提交答案")` 返回False，导致整个代码块不执行，`quiz_idx += 1` 永远不会被执行。

**修复方案**:
使用 session_state 标志位来追踪答题状态。具体逻辑:

1. 添加 session_state 初始化 (行50-61区域):
```python
if "answer_result" not in st.session_state:
    st.session_state.answer_result = None  # None | {"is_correct": bool, "correct_answer": str}
```

2. 重写 quiz_active_page() 逻辑:

```
- 如果 answer_result 为 None (未提交状态):
    - 显示题目 + 选项 (radio)
    - 显示"提交答案"按钮
    - 点击后: 调API → 更新score → 设置 answer_result → rerun

- 如果 answer_result 不为 None (已提交，显示结果):
    - 显示题目 + 选项 (radio, disabled=True 不可更改)
    - 显示对错结果 (success/error + 正确答案)
    - 显示"下一题"按钮 (不嵌套在任何条件下)
    - 点击"下一题": quiz_idx += 1, answer_result = None, rerun
```

关键改动: "下一题"按钮必须在所有条件分支之外，作为顶层元素渲染。

---

#### Bug 2: 课程中心点击模块卡片无反应

**文件**: streamlit_app.py, courses_page() 函数 (行454-499)

**根本原因**:
- 模块卡片用HTML `<div class="module-card">` 渲染，无点击事件
- "阅读"按钮 (行481) 只调用 `log_reading()` 记录日志，不进入模块详情
- 缺少模块详情页面和路由

**修复方案**:

1. 将模块卡片从纯HTML改为Streamlit按钮，使模块可点击进入详情:
   - 替换每个模块的 `st.markdown("<div class='module-card'>...")` 为 `st.button` 或使用 `st.container()` + 按钮

2. 添加 session_state:
```python
if "course_module" not in st.session_state:
    st.session_state.course_module = None  # 当前查看的模块代码
```

3. 在页面路由字典中添加新页面:
```python
"module_detail": module_detail_page,
```

4. 侧边栏添加返回按钮逻辑(当在module_detail时)

---

### P1 — 重要: 课程中心学习框架

#### 为每个模块构建学习框架

点击模块卡片后，进入 module_detail_page，显示该模块的学习框架:

**模块详情页应包含**:

1. **模块标题区**: 模块名 + 难度等级badge + 描述
2. **学习目标卡片** (3-5条): 学完本模块后应掌握的技能
3. **核心知识点区** (树形结构): 按主题分组的子知识点
4. **相关文献列表**: 该模块下的PDF文献 (已有数据)
5. **相关题目**: 该模块有多少题，可练习按钮
6. **详细内容链接**: 占位，标注"📝 详细内容开发中"

**学习框架数据来源**:

在 streamlit_app.py 中定义 MODULE_FRAMEWORK 字典，格式如下:
```python
MODULE_FRAMEWORK = {
    "00-fabrication": {
        "objectives": [
            "理解硅晶体生长(CZ/FZ)与晶圆制备流程",
            "掌握Fab基本术语与洁净室概念",
            "了解CMOS工艺全流程概览",
            "理解半导体产业链分工"
        ],
        "topics": [
            {"name": "半导体物理基础", "subs": ["能带理论", "掺杂原理", "载流子输运"]},
            {"name": "硅材料与晶圆", "subs": ["CZ法单晶生长", "FZ法", "晶圆规格参数", "SOI衬底"]},
            {"name": "CMOS工艺流程", "subs": ["前段FEOL", "后段BEOL", "工艺流程概览"]},
            {"name": "Fab运营基础", "subs": ["洁净室ISO等级", "WIP与Little定律", "设备利用率OEE"]},
        ]
    },
    "13-photolithography": {
        "objectives": [
            "掌握光刻基本原理与衍射极限公式",
            "理解DUV/EUV光刻光源系统差异",
            "了解光刻胶化学与涂布显影工艺",
            "理解OPC/RET分辨率增强技术",
            "掌握关键光刻参数(CD/Overlay/DOF)"
        ],
        "topics": [
            {"name": "光刻基本原理", "subs": ["衍射极限 R=k1·λ/NA", "曝光系统光学", "焦深DOF"]},
            {"name": "光源系统", "subs": ["DUV 193nm ArF", "EUV 13.5nm", "浸没式光刻"]},
            {"name": "光刻胶", "subs": ["正胶/负胶", "化学放大胶CAR", "涂布与软烘", "显影工艺"]},
            {"name": "分辨率增强", "subs": ["OPC光学邻近修正", "相移掩模PSM", "离轴照明OAI", "多重曝光"]},
            {"name": "先进光刻", "subs": ["EUV掩模", "High-NA EUV", "DSA导向自组装", "纳米压印NIL"]},
        ]
    },
    "11-thin-film": {
        "objectives": [
            "区分PVD/CVD/ALD三种沉积技术原理",
            "掌握溅射与蒸发的基本工艺参数",
            "理解ALD自限制反应机制",
            "了解薄膜应力控制与附着力"
        ],
        "topics": [
            {"name": "PVD物理气相沉积", "subs": ["磁控溅射", "电子束蒸发", "离子束沉积", "台阶覆盖率"]},
            {"name": "CVD化学气相沉积", "subs": ["LPCVD", "PECVD", "MOCVD", "前驱体化学"]},
            {"name": "ALD原子层沉积", "subs": ["自限制反应", "TMA/H2O Al2O3", "PEALD", "前驱体选择"]},
            {"name": "薄膜表征", "subs": ["椭偏仪测厚", "XRR", "应力测量", "附着力测试"]},
        ]
    },
}
```

为所有27个模块定义框架数据。对于没有详细框架的模块，提供通用框架: objectives=["掌握本模块核心知识", "理解关键概念与应用", "能够独立分析相关问题"], topics=[{"name": "核心内容", "subs": ["待细化"]}]。

**模块详情页实现** - 新增 module_detail_page() 函数:

```python
def module_detail_page():
    module_code = st.session_state.course_module
    if not module_code:
        st.session_state.page = "courses"
        st.rerun()
    
    # 从MODULE_META获取模块元数据
    from app.config import MODULE_META
    meta = MODULE_META.get(module_code, {"name": module_code, "desc": "", "level": 200})
    framework = MODULE_FRAMEWORK.get(module_code, {})
    
    # 返回按钮
    if st.button("← 返回课程中心"):
        st.session_state.course_module = None
        st.session_state.page = "courses"
        st.rerun()
    
    st.title(f"📖 {meta['name']}")
    level_badge = {100: "🟢 L1 基础", 200: "🟡 L2 应用", 300: "🟢 L3 深入", 400: "🔴 L4 专家"}
    st.caption(f"{level_badge.get(meta['level'], 'L2')} | {meta['desc']}")
    
    # 学习目标
    st.subheader("🎯 学习目标")
    objectives = framework.get("objectives", ["掌握本模块核心知识"])
    for i, obj in enumerate(objectives, 1):
        st.markdown(f"{i}. {obj}")
    
    st.divider()
    
    # 核心知识点
    st.subheader("📚 知识框架")
    topics = framework.get("topics", [])
    for topic in topics:
        with st.expander(f"📂 {topic['name']}", expanded=False):
            for sub in topic.get("subs", []):
                st.markdown(f"• {sub}  `📝 详细内容开发中`")
    
    st.divider()
    
    # 相关资源
    col1, col2 = st.columns(2)
    with col1:
        st.subheader("📄 文献资源")
        index = api("/knowledge/index")
        if index and module_code in index:
            mod_data = index[module_code]
            for pdf in mod_data.get("pdfs", [])[:10]:
                st.caption(f"📄 {pdf['name'][:70]}")
            if len(mod_data.get("pdfs", [])) > 10:
                st.caption(f"... 共 {len(mod_data['pdfs'])} 篇")
    with col2:
        st.subheader("✏️ 相关练习")
        # 查询该模块题目数
        if st.button("🚀 开始练习", key=f"quiz_{module_code}", use_container_width=True):
            from app.config import MODULE_META as mm
            quiz = api("/quiz/generate", "POST", {
                "user_id": st.session_state.user["user_id"],
                "module_code": module_code,
                "count": 10,
                "difficulty": None
            })
            if quiz and quiz.get("questions"):
                st.session_state.quiz = quiz["questions"]
                st.session_state.quiz_idx = 0
                st.session_state.quiz_score = 0
                st.session_state.quiz_module = module_code
                st.session_state.page = "quiz_active"
                st.rerun()
```

## 技术约束

- **不要修改 app/config.py、app/main.py、app/database.py、app/quiz.py** — 只修改 streamlit_app.py
- **不要破坏现有API端点**
- **保持深色主题CSS不变**
- **使用中文UI**
- **保持现有的session_state变量名不变，只新增**
- **所有MODULE_FRAMEWORK数据直接写在streamlit_app.py中**
- **courses_page()中的"阅读"按钮保留，但旁边新增"进入模块"按钮**

## 修改清单 (仅 streamlit_app.py)

### 1. session_state 初始化区 (行50-61后添加):
- `answer_result` = None
- `course_module` = None

### 2. 新增 MODULE_FRAMEWORK 字典 (放在 session_state 初始化后, 约行62后):
- 为全部27个模块定义框架 (已在上面列出部分，其余用通用框架)
- 至少包含: 00-fabrication, 13-photolithography, 11-thin-film, 14-dry-etch, 15-metrology, 16-CMP, 18-IMP, 20-PKG 的详细框架，其余用通用框架

### 3. quiz_active_page() 重写 (行625-670):
- 根据 answer_result 是否为None分两种状态
- 未提交: 显示题目+选项+提交按钮
- 已提交: 显示结果+禁用选项+下一题按钮(独立渲染)

### 4. courses_page() 修改 (行454-499):
- 每个模块卡片下方添加"进入模块"按钮
- 点击后: 设置 course_module → page="module_detail" → rerun

### 5. 新增 module_detail_page() 函数:
- 放在 courses_page() 后面 (约行500前)

### 6. 路由字典更新 (行961-971):
- 添加 "module_detail": module_detail_page

### 7. 侧边栏 (行377-402):
- 当 page == "module_detail" 时不显示侧边栏导航(或显示返回按钮)

### 8. quiz_select_page() 调整 (行502):
- 如果 answer_result 存在但quiz未开始，应清除 answer_result

## 验证方式

```bash
# 1. 语法检查
python3 -c "import py_compile; py_compile.compile('~/Projects/training-system/streamlit_app.py', doraise=True)"

# 2. 检查关键函数是否存在
grep -n "def module_detail_page\|def quiz_active_page\|MODULE_FRAMEWORK\|answer_result\|course_module" ~/Projects/training-system/streamlit_app.py

# 3. 检查路由是否包含新页面
grep -n '"module_detail"' ~/Projects/training-system/streamlit_app.py

# 4. 统计MODULE_FRAMEWORK有多少个模块
python3 -c "
import ast, sys
with open('$HOME/Projects/training-system/streamlit_app.py') as f:
    tree = ast.parse(f.read())
for node in ast.walk(tree):
    if isinstance(node, ast.Assign) and hasattr(node.targets[0], 'id') and node.targets[0].id == 'MODULE_FRAMEWORK':
        print(f'MODULE_FRAMEWORK keys: {len(node.value.keys)}')
"

# 5. 检查"下一题"按钮不在提交按钮嵌套内
# 手动审查: 确保 st.button('下一题') 与 st.button('提交答案') 同级缩进
```
